# Managed component dependencies for the `src` component.
#
# This file is consumed by ESP-IDF's component manager and controls
# registry-downloaded dependencies for this component only.
# Pin explicit versions when deterministic CI reproducibility is required.
dependencies:
  espressif/led_strip: "*"
